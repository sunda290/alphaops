// Registro de eventos de lead — placeholder para implementação futura
// Quando implementado: salvar em db.collection('lead_eventos')
export async function registrarEvento(_leadId: string, _evento: string, _canal = 'sistema') {
  // TODO: implementar persistência de eventos no Firestore via firebase-admin
}
